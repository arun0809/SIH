import 'package:flutter/material.dart';
class StudentappStatus extends StatefulWidget {
  const StudentappStatus({Key? key}) : super(key: key);

  @override
  _StudentappStatusState createState() => _StudentappStatusState();
}

class _StudentappStatusState extends State<StudentappStatus> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
